// FuelStats and FuelTable are no longer used as standalone components.
// FuelManagement.jsx now manages its own state and renders everything inline.
// These files are kept for import compatibility but export nothing that renders mock data.
export default function FuelStats() { return null; }